
using OpenTK.Windowing.GraphicsLibraryFramework;
using OsmiumNucleus;
using OsmiumRadium;


namespace OsmiumEditor;
/*

public class CreateScenePopup : RadiumElement
{
    public string SceneName = "";

    protected override void Draw() {
        SceneName += Backend.TextInput;

        //todo: blinking line for _text input??? and make htis a radium thing and fix myu naming conventoins and do everything in the whole wide owr;d
        var BackgroundBox = new Box(new Bounds(size: new Vector2(10, 10), center: Vector2.One * 50), Palette.BackgroundLow);

        //todo: make _text support _bounds somehow
        var OptionText = new Text(SceneName, center: Vector2.One * 50);

        if (Osmium.Context!.KeyboardState.IsKeyDown(Keys.Enter)) {
            Osmium.AddScene(SceneName);
            
            //todo: guard multiple popups and redo code
            //radium reference remove option
            Radium.Remove<CreateScenePopup>();
        }
    }
}
*/